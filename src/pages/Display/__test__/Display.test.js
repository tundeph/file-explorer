import { fireEvent, render, screen } from "@testing-library/react"
import Display from "../Display"

describe("Display Tests", () => {
  it("should render components on the page", () => {})

  it("should check only one sorting feature is clicked per time", () => {})

  it("should filter when text is typed in the file filter input", () => {})
})
